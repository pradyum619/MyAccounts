<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.sharmaji.myaccounts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyAccounts">
        <activity
            android:label="@string/customer_detail"
            android:name=".activities.CustomerDetailActivity"
            android:exported="true" />
        <activity
            android:name=".activities.AddCustomerActivity"
            android:exported="true"
            android:label="@string/add_customer"
            android:theme="@style/addCustomerActivityTheme"
            android:windowSoftInputMode="stateVisible"
            android:noHistory="true"/>
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>